# LEGAL Tools

Primary surfaces:
- user-supplied contract and policy inputs
- public-safe templates and instructions
- optional document tooling supplied by the installer

Suggested dependencies:
- Python 3.11+
- optional document tooling
- optional external research keys supplied post-install

Privacy contract:
- never ships privileged documents
- never assumes one operator's legal workflow
