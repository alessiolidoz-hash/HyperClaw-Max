# FINANCE Overlay

Finance should be treated as an optional overlay.

Reason:
- finance logic is useful
- but much of the current private lane depends on personal data, workflows, and sources not suitable for the default public install
